export default function DashboardPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Streamed role-aware widgets arrive in the dashboard phase.
      </p>
    </section>
  );
}
